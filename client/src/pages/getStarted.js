import React, { useState } from "react";
import Layout from "../layout/Layout";

const GetStarted = () => {
    return (
        <Layout>
            <h1>Get Starter</h1>
        </Layout>
    );
};

export default GetStarted;
